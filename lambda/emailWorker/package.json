{
  "name": "order-email-worker",
  "version": "1.0.0",
  "description": "AWS Lambda worker for sending order confirmation emails via SES triggered by DynamoDB Streams.",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc"
  },
  "dependencies": {
    "@aws-sdk/client-ses": "^3.956.0",
    "@aws-sdk/util-dynamodb": "^3.956.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "@types/node": "^20.11.0",
    "typescript": "^5.3.3"
  }
}
